# MongoDB Atlas Connection String
# Get this from: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?options
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/todo-list-api?retryWrites=true&w=majority

# JWT Secret for token signing (use a strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<generate-random-secret-key-here>

# JWT Token expiration time
JWT_EXPIRATION=30d

# Server port
PORT=3000

# Environment (development, test, production)
NODE_ENV=development
